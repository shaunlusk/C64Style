TODO

--X--? move current event creation of mouse|element events to separate
for buttons/links/gfxElement mouse event handling:
_getElementsForMouseEvent - change to propagateEventAndReturnElements
element function - handle mouse event
  collision
  return true if collision
  listener code

  element events
    mouse over
    mouse down
    mouse up

screen
  store bg, border colors
  separate element mouse down events? - separate from mouse down/up/over?

throw error on unimplemented methods in layer - but not propagateEvent

demos
  text input
  sprite
  image sprite
  drag and drop
  mouse over

per layer events
  before / after render




webpage intro

documentation

license
functionality and unit tests for UniquePriorityQueue

loading animation

known issue: in text layer, strings that are rendered in same frame may be written on top of eachother without clearing (clearing comes before drawing)

add additional char maps
  person
  caret
  tilda
  curly braces

textSprite?

tests for
  priority queue
  c64screen
  colorpointer
  gfxElement
  gfxElementzindex comparable
  gfx layer
  layer
  piximage
  text layer

element finalize

Bounds checking on text layer?
Text wrapping on text layer?
